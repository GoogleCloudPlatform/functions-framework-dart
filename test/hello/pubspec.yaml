name: hello_world_function_test
publish_to: none

environment:
  sdk: ">=2.10.0 <3.0.0"

dependencies:
  functions_framework: any
  json_annotation: ^3.1.1
  pedantic: ^1.9.2
  shelf: ^0.7.9

dev_dependencies:
  build_runner: ^1.10.2
  build_verify: ^1.1.1
  functions_framework_builder: any
  http: ^0.12.0
  http_parser: ^3.1.4
  io: ^0.3.4
  json_serializable: ^3.5.1
  # Because we use the ignore_for_file build.yaml config
  source_gen: ^0.9.5
  test: ^1.15.0
  test_process: ^1.0.0

dependency_overrides:
  functions_framework:
    path: ../../functions_framework
  functions_framework_builder:
    path: ../../functions_framework_builder
