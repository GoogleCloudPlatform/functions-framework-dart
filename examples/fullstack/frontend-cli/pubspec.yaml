name: frontend_cli
publish_to: none

environment:
  sdk: '>=2.12.0 <3.0.0'

executables:
  greet:

dependencies:
  http: ^0.13.1
  io: ^1.0.0
  json_annotation: ^4.0.1

dev_dependencies:
  build_runner: ^1.12.2
  json_serializable: ^4.1.0
  pedantic: ^1.9.0
  test: ^1.14.4
