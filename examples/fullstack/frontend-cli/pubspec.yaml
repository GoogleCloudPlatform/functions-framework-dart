name: frontend_cli
publish_to: none

environment:
  sdk: ^3.0.0

executables:
  greet:

dependencies:
  http: '>=0.13.1 <2.0.0'
  io: ^1.0.0
  json_annotation: ^4.8.1

dev_dependencies:
  build_runner: ^2.0.0
  dart_flutter_team_lints: ^1.0.0
  json_serializable: ^6.0.0
  test: ^1.14.4
